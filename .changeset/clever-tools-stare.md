---
'maze': patch
---

Adding CI checks for code quality. Migrating from Yarn -> pnpm and Jest -> Vitest. Adding changesets and support for a new `feature branch` -> `dev` -> `main` release workflow supported by changesets.
